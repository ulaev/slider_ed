'use strict';

import slider from './slider.js'

slider.init({
    id: 'slider1',
    width: 400,
    height: 300,
    autoPlayLeft: true,
    interval: 1000,
});
slider.init({ id: 'slider2', width: 800, height: 600 });
